import ResetPasswordForm from "@/components/auth/ResetPasswordForm";

export const metadata = {
  title: "Behold — Set New Password",
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
